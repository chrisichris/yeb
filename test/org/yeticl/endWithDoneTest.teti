module org.yeticl.endWithDoneTest;

import java.lang.StringBuffer;

do stBi:

    stB is ~StringBuffer = stBi;

    write str = (stB#append("\(str)"); ());
    writers = { e = write, u = write};

%>Does end with done?