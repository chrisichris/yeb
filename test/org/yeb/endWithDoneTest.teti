<%module org.yeb.endWithDoneTest;

import java.lang.StringBuffer;

do stBi:

    stB is ~StringBuffer = stBi;

    write str = (stB#append("\(str)"); ());
    
    writer = {
        escaped = write,
        unescaped = write
    };
    
    %>Does end with done?<%
done