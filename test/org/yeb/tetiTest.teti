<%
module org.yeb.tetiTest;

import java.lang.StringBuffer;

stB = new StringBuffer();

write str = (stB#append("\(str)"); ());

writer = {
    escaped = write,
    unescaped = write
};


%>Hier kommt Kurt.<%

stB#toString();
