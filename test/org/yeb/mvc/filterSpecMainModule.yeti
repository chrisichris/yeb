/// put module documentation here

module org.yeb.mvc.filterSpecMainModule;

load org.yeb.mvc.handler;
config = load org.yeb.mvc.config;

{
    init _ = (config.messageBundles := ["init yebMain"]),
    destroy _ = (),
}